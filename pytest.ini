[pytest]
testpaths = tests
python_files = test_*.py *_test.py
addopts = -m "not integration and not external and not visual"
markers =
    integration: tests requiring local services, browser automation, or non-default fixtures
    external: tests requiring a real external network provider or court website
    visual: tests requiring browser screenshots or visual inspection
